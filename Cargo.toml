[package]
name = "memory_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "memory_core"
crate-type = ["cdylib"]   # required for Python extension

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module", "macros"] }
rand = "0.9.2"
