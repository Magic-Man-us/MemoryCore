[package]
name = "memory_core"
version = "0.2.0"
edition = "2021"

[lib]
name = "_native"
crate-type = ["cdylib"]   # required for Python extension

[dependencies]
pyo3 = { version = "0.24", features = ["extension-module", "macros"] }
